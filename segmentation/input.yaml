# HECKTOR22 Auto3dseg minimal user input

modality: MRI                        # primary modality
dataroot: ../data/augmented/normalized2mm           # dataset location
datalist: folds.json      # a list of filenames
class_names: [tumor, lymph_node]    # names for tensorboard

